<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <form method="POST" action="/movies/{{notesId}}/reviews">
            <fieldset>
                <input type="hidden" id="notes-title" type="text" name="notesTitle" value="{{notes.title}}"/>

                {{> notes-form}}

            </fieldset>
            <!--BUTTON-->
            <div>
                <div>
                    <form method="POST" action="/notes">
                    <!-- BUTTON -->
                        <div class="form-group">
                            <button class="btn btn-primary" type="submit">Save Note</button>
                        </div>
                    </form>
                </div>
            </div>
        </form>
    </div>
</div>
  
